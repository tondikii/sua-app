import { ComingSoon } from '../../../src/components/ComingSoon';

/** Media tab (R2). Implemented in M14 (Media §10). */
export default function MediaTab() {
  return <ComingSoon label="Media — implemented in M14" />;
}
