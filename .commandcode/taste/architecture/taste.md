- When an activity's wall-clock end time is earlier than its start time (e.g., 13:00→12:00 spanning midnight), the itinerary timeline must detect `spansMidnight` and adjust rendering: extend the day's window to 00:00–24:00, sort cross-day activities last in the timeline, skip gap/trailing calculations after a midnight-spanning activity, and treat `resolveItineraryTimeState` so both `now >= start` and `now < end` return 'present'. Without this, the timeline defaults to a 07:00–20:00 window that clips and misplaces cross-day activities. Confidence: 0.9

- The itinerary day window badge must use the trip-level `start_time`/`end_time` (wall-clock HH:MM from the trips table) as its base window, not hardcoded `DEFAULT_WINDOW_START`/`DEFAULT_WINDOW_END` (07:00/20:00). `ItineraryTimeline` must accept `tripStartTime`/`tripEndTime` props and pass them to `buildItineraryDays`; `buildItineraryDays` uses these as the initial `windowStart`/`windowEnd` before activities widen the window. When trip-level times are absent, fall back to the defaults. Confidence: 0.92# architecture
- Prefers to audit and fix monorepo architecture fundamentals (package boundaries, shared code, tooling) before building features on top — takes a "foundations first" approach rather than deferring structural debt. Confidence: 0.9
- Prefers shared packages across backend and mobile (shared types, shared validation schemas) instead of duplicating type definitions or validation logic in each package. Confidence: 0.85
- Prefers standardized root-level tooling configs (ESLint flat config, Prettier, tsconfig.base.json) applied uniformly across all packages in the monorepo rather than per-package bespoke configurations. Confidence: 0.8
- Prefers making architectural decisions via structured options with clear recommendations, and tends to choose the comprehensive option when presented. Reconfirmed when picking the "Backend + cache (Recommended)" scope over the backend-only or mobile-cache-only partial options for the avatar fix. Confidence: 0.85
- Expects documentation (README, env examples, architecture docs) to be updated alongside code changes — does not tolerate stale docs after refactoring. Confidence: 0.8
- Expects the agent to read ALL relevant documentation files comprehensively (README, PRD, WORKFLOW, FIGMA, ARCHITECTURE, etc.) before starting work — being reminded to read a missing doc is a signal of insufficient thoroughness. Confidence: 0.85

- Every React Query hook that fetches user-facing data should include `refetchOnMount: 'always'` to bypass stale cache from AsyncStorage persistence — applied consistently to `useTrips`, `useUserTrips`, etc. Confidence: 0.75

- Screens that display API-fetched data must have explicit loading state (e.g., `ActivityIndicator`) and error state (e.g., fallback UI with retry option) — no screen should silently show empty content while loading or on error. Confidence: 0.8

- Every user-triggered API call — including action/mutation endpoints (e.g., sending a trip invite), not just data-fetching screens — must show an explicit loading indicator while the request is in flight. A missing loader on any API hit is a bug the user expects the agent to audit systematically across the whole app ("tolong audit lagi segala hit api yang belum ada loadernya" — audit all API hits lacking loaders; for list-item actions (e.g., accept/decline invitation), the loader must be scoped to that item's own buttons — a spinner inside the button while its request is in flight, not a global spinner). Confidence: 0.85

- Prefers real-time derived values (e.g., `trips.length` from fresh query data) over stale cached counters (e.g., `user.trip_count` from one-time session hydrate) for stat displays in the UI. Confidence: 0.75

- Proactively cleans up unused imports and merges duplicate imports from the same module (e.g., `import { colors } from '@/theme/colors'` + `import { avatarColorFor } from '@/theme/colors'` → single import) when editing a file. The user also explicitly requests dead/unused code removal as part of change requests ("hapus code code terkait wishlist yang sudah tidak diperlukan"), so the agent should audit for and strip unused imports/utilities (e.g., `useRef`, `AsyncStorage`, `useConvertToTrip`, unused theme imports) rather than leaving them behind. Confidence: 0.8

- When Prisma has version-specific bugs (e.g., 5.22 + Node 24 `take` validator regression), prefers falling back to `$queryRawUnsafe` with hand-written SQL rather than endlessly trying different ORM syntax permutations — pragmatic "bypass the bug" approach over "work around within the ORM." Confidence: 0.7

- Prefer popup/dropdown menus over full-screen navigation for secondary management actions (e.g., Kelola Trip / kebab ⋯ actions should be a popup menu from the header, not a separate routed screen). Confidence: 0.9

- Related sub-features of a detail screen (e.g., Voting, Chat, Media inside Trip Detail) should be rendered inline in the same screen via local tab state — not as separate routed screens each with their own header/back navigation; the URL should not change when switching tabs. Confidence: 0.85

- Edit screens should reuse the create screen component with an edit state flag — the user explicitly stated edit-info should go to "the same screen as create trip but set conditional" ("edit info perjalanan harusnya ke screen yang sama kaya buat perjalanan tapi diset conditional", citing Figma Screen103TripEdit): prefill existing data, swap the CTA to "Simpan", and reuse the same form shell rather than building a separate near-duplicate screen or a small popup. Confidence: 0.92

- Activity creation form should not include a "Jenis" (kind/type) selector field — the kind is determined by context or defaults per the design spec; extra fields not in the design are bugs. Confidence: 0.85

- Backend API validation should defensively trim whitespace from time inputs before applying strict format validation (e.g., `.trim()` on start_time and end_time before Zod regex check) to handle client-side formatting inconsistencies. Confidence: 0.8

- Date-only fields (e.g., `start_date`, `end_date` for poll/tanggal options) must use strict `YYYY-MM-DD` format validated via regex (`/^\d{4}-\d{2}-\d{2}$/`) rather than full ISO datetime validation (`z.string().datetime()`) — the user's API payloads send date-only strings and the schema must accept them, not require a time component. Confidence: 0.8

- When data from a relaxed validation context needs to query a database column with a stricter type constraint (e.g., UUID primary key), prefer an application-level format guard (e.g., regex check) that gracefully skips or handles non-matching values, rather than relaxing the database constraint or letting the database throw a cryptic error. Confidence: 0.7

- When a poll option carries date candidate data (start_date/end_date) but no corresponding database record exists, automatically create the `TripDateCandidate` record at poll creation (or update) time so that downstream operations (e.g., locking the poll → resolving the winning candidate → updating the trip's startDate/endDate) resolve correctly — prefer creating the backing record over skipping/guarding the lookup when the data is needed for a business operation. This must run within the same `$transaction` as the poll/option creation. The user explicitly expects "setelah voting tanggal dikunci, tanggal yang menang voting menjadi tanggal perjalanan." Confidence: 0.7

- Calendar date picker should default to today's date as the initial selected date when first opened. Confidence: 0.85

- Navigation back actions should gracefully handle an empty navigation stack (e.g., after browser refresh) by falling back to a default route instead of throwing an error. Confidence: 0.85

- When a component's presentation layer changes (e.g., from modal sheet to full screen), prefers refactoring the existing component into a pure content component (no modal/backdrop/visible props) and wrapping it in a new routed screen that provides the header/navigation — reusing the existing content logic rather than rewriting it, and deleting the obsolete modal file. Confidence: 0.75
- User-entered times are wall-clock values (e.g., 19:00–21:30) that must be preserved and rendered identically on every surface (form, wishlist, itinerary activity, serializers) — any timezone-induced shift (e.g., a Prisma `@db.Time` value read with local `getHours()`/`toTimeString` vs UTC `toISOString` turning 19:00 into 07:00) is a bug the user will report. Times should be treated as local wall-clock throughout DB storage and serialization, never as UTC instants; when a time renders differently in one part of the app than another, the agent must trace the DB value → serializer → UI chain to find the conversion mismatch. Confidence: 0.7

- Backend API responses must use snake_case field naming (matching the frontend TypeScript types) — Prisma camelCase fields (e.g., `avatarUrl`, `startDate`) must be mapped to snake_case (`avatar_url`, `start_date`) in serializers/controllers before sending to the client. Raw Prisma objects must never be returned directly in API responses. Confidence: 0.9

- Third-party SDK clients must be configured to produce browser-friendly presigned URLs: when an AWS SDK default (e.g., S3 v3.645+ `requestChecksumCalculation: 'WHEN_SUPPORTED'`) stamps a non-simple header (e.g., `x-amz-checksum-crc32=AAAAAA==`) onto presigned PUT URLs, browsers fire a CORS preflight that the object store rejects — the fix is to set `requestChecksumCalculation: 'WHEN_REQUIRED'` (or equivalent) so presigned URLs stay simple-header and uploads work from the web. Root-cause diagnosis must account for how the running system actually behaves (SDK version, resolved config defaults), not just the reported error. Confidence: 0.8

- Trip membership management must include a leave/exit feature for non-creator members — non-creators should be able to leave a trip they've joined, with a proper confirmation modal (matching the Figma design for destructive actions) and a kebab menu item "Keluar dari Perjalanan" with a LogOut icon. This is a standard membership feature that must be exposed in the UI. Confidence: 0.8

- Invite UI must exclude users who are already trip members from the inviteable/search list — not just users with pending invitations. Both the member list and the invite bottom sheet must check against the current members roster and show "✓ Anggota" badge for existing members (distinct from "✓ Terundang" for pending invitations). Confidence: 0.85

- Scheduled items inside a date-ranged parent entity (e.g., trip activities) must be anchored to a relative day index (day_number) rather than an absolute date — the displayed date is computed from the parent's start date + (day_number - 1), so when the parent's dates change the items shift automatically instead of disappearing from view. The user explicitly required this after activities vanished when trip dates changed: "aktivitas nempel di hari gitu bukan date time sendiri karena nanti ga tampil kalo tanggal perjalanannya berubah" (activities should stick to a day, not their own date, or they won't show when the trip date changes). Confidence: 0.85

- Media/covers generated for a sub-entity (e.g., an activity's cover from a Google Maps thumbnail or the device gallery) should automatically flow into the parent entity's (trip) media collection — once in the trip media, that same image becomes reusable as the trip's cover. The user proposed this mechanism explicitly as the way to make activity covers usable as trip covers ("cover aktivitas yang dari maps dan galeri perangkat auto masuk ke media perjalanan. kalo sudah masuk media perjalanan seharusnya bisa dijadikan cover"). Confidence: 0.8

- Supabase Realtime `postgres_changes` payloads are raw DB rows (e.g., `sender_id`, un-presigned `media_url`, no joined relations) — never merge them directly into the TanStack Query cache; invalidate the REST query (`qc.invalidateQueries`) so the refetch returns the hydrated API shape (joined `sender`, presigned `media_url`, recomputed `unread_count`). Appending raw rows produces broken UI (missing sender names, dead media URLs) — the subscription should invalidate on INSERT/UPDATE rather than append/merge payloads. Confidence: 0.85

- External side-effect integrations (push notifications, background thumbnail resolution) must be fire-and-forget: never block the HTTP response, failures are only logged (warn), and when the required credential is missing (e.g., `EXPO_ACCESS_TOKEN` unset in dev) the push degrades to a logged no-op while the core in-app functionality still works — the agent followed the established `scheduleThumbnailResolve` pattern in `activity.service` when building `PushNotificationsService`. Confidence: 0.65

- Prefers bulk/batch DB writes over N+1 loops: when notifying many recipients for a single event (e.g., activity create/update → all participants, voting reminder → all non-voters), use one `createMany` with `skipDuplicates: true` instead of `Promise.all(map(createNotification))`, and batch relation lookups (users/trips) rather than per-row queries — the approved "hardening" pass explicitly replaced per-participant `createNotification` loops with a single batched call. Confidence: 0.6

- Persisted references to object storage (DB columns, message payloads) must be storage keys or canonical public URLs — never short-lived presigned URLs (they expire and get mangled when re-extracted for re-presigning). When the backend auto-registers the record (e.g., chat's `from_chat=true` document), the client should only presign + PUT to R2 and send the storage key as `media_url` — single registration responsibility, never double-register the same object (a client-registered `trip_documents` row plus a backend-created one) and never persist a presigned GET URL with `?X-Amz-...` query params. For private buckets (no public r2.dev access — probe the actual URL to confirm: public r2.dev host fails to connect, no-sig S3 endpoint returns 400, presigned GET returns 200), this means the DB column (e.g., `User.avatarUrl`) stores the storage key (`avatars/{userId}/...`) and serializers resolve it to a presigned GET at response time — never persist `pub-*.r2.dev` URLs that browsers cannot render. Resolvers must handle three URL shapes: storage key → presign; legacy stored `pub-*.r2.dev` URL → extract the path and presign (existing data fixed at read time, no migration); external URL (e.g., Google `lh3.googleusercontent.com`) → pass through untouched. This presign-at-serialization rule must be applied UNIFORMLY at every output point — when an avatar bug appeared in 4 places at once (member page, trip cards, voting cards, search results), the root cause was that only `UserSummarySerializer.toProfile` presigned while every other serializer (trip, poll, invitation, message, search, notifications) returned the raw private-bucket key; the fix was to route all of them through the same `resolveAvatar`, batch-presign lists via `r2.presignDownloads` to avoid N+1, and wire `R2Service` into every service that serializes avatars. Confidence: 0.9

- When an activity's wall-clock end time is earlier than its start time (e.g., 13:00→12:00 spanning midnight), the itinerary timeline must detect `spansMidnight` and adjust rendering: extend the day's window to 00:00–24:00, sort cross-day activities last in the timeline, skip gap/trailing calculations after a midnight-spanning activity, and treat `resolveItineraryTimeState` so both `now >= start` and `now < end` return 'present'. Without this, the timeline defaults to a 07:00–20:00 window that clips and misplaces cross-day activities. Confidence: 0.9

- When converting a wishlist to a trip (convertToTrip), the wall-clock times (`start_time`/`end_time`) must be persisted to the `trips` table (not only to `trip_activities`), so the trip detail header displays the correct times. The request's `start_time`/`end_time` take priority over the wishlist's stored times; if neither is provided, the trip-level times are null. Confidence: 0.85

- When converting a wishlist to a trip, the wishlist's `thumbnailUrl` (Google Maps place photo) must be auto-imported into R2 as a `tripDocument` and set as both the trip's `coverDocumentId` and the seeded activity's `coverDocumentId`/`coverSource`. This is best-effort (never fails the trip creation) and mirrors the existing `importRemoteThumbnailToTripMedia` pattern in `activity.service`. Confidence: 0.85` and set as both the trip's `coverDocumentId` and the seeded activity's `coverDocumentId`/`coverSource`. This is best-effort (never fails the trip creation) and mirrors the existing `importRemoteThumbnailToTripMedia` pattern in `activity.service`. Confidence: 0.85dari jam 1 siang sabtu sampe minggu jam 12 siang"). The DB check constraint `valid_activity_time` on `trip_activities` was dropped via migration so wall-clock times can cross midnight; validation must never reject `end_time < start_time`, and consumers (timeline window, serializers, time-state) must handle it. Confidence: 0.7

- OAuth callback redirects must return to the exact return URL the client passed to the auth session — native deeplink (e.g., `aturperjalanan://trip/{id}`) or web origin — not a hardcoded web URL. When the backend redirects to the web app while mobile awaits the deeplink return URL, `openAuthSessionAsync` never reports success, so post-consent steps (e.g., create calendar event) silently never run and the user sees "no success feedback, no event created". Store the client-supplied full redirect URI in the OAuth `state` and redirect to it verbatim (bare paths get prefixed with the app web URL). Confidence: 0.8

- Prefers free-tier hosting services for initial production deployment (web + backend) before scaling to paid tiers — explicitly chose Render free (backend) and Cloudflare Pages (static web) as zero-cost deployment targets, accepting free-tier tradeoffs (e.g., service cold starts after idle, limited concurrency) as acceptable for early release rather than enabling paid hosting. Confidence: 0.8

- Enforces a soft cap on registered users via a configurable backend gate (`USER_LIMIT` env var, default 50) rather than a hard freeze — new sign-ups are rejected with a structured `USER_LIMIT_REACHED` error once active users reach the threshold, while existing users can always sign in. Applied at the auth registration boundary (not on login) so the free-tier capacity ceiling is respected without locking out real users; the limit is env-driven so capacity can be raised without a new deploy. Confidence: 0.85

- For deadline/time-based reminders (voting deadline, trip start), prefers proportional scheduling over fixed hardcoded lead times: reminder targets should be computed as a fraction of the actual gap between an anchor (e.g., `trip.updatedAt`) and the deadline (e.g., R1 at 50%, R2 at 25% of the gap) with absolute minimum-lead floors (30m/5m), because hardcoded horizons (H-7d/H-1d/H-1h) silently miss short deadlines (e.g., a voting deadline set 30 minutes after creation gets no reminder at all). Also prefers a small number of high-signal reminders — the user explicitly chose exactly 2 per deadline ("aku mau remindernya 2x saja") over a cascade. Confidence: 0.8