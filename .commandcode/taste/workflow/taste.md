duplication in the composer is a bug to fix in the shipped output, not left as-is. Confidence: 0.6

- Use pnpm (not npm) as the package manager throughout this project — it is a pnpm workspace (`pnpm-workspace.yaml`), and npm causes platform-specific failures (e.g., husky install errors on Windows). The user explicitly confirmed pnpm ("run figmanya pake pnpm") when the agent initially used npm and got stuck. Confidence: 0.9

- When auditing a design reference app (e.g. the `figma/` Vite preview registry), run it locally via pnpm (`cd figma && pnpm run dev`) to confirm it actually boots and renders all screens without console errors — do not treat the design registry (`App.tsx`, `colors.ts`, `Screen*`) as a read-only reference. The audit must verify the reference preview works live, and this pairs with lint + typecheck/build verification on the implementation (Expo) side as the minimum bar before declaring screen alignment. Confidence: 0.8# workflow
- Prefers agent to first read project documentation (README, docs/) to understand context before proposing next steps or architecture decisions — "read first, propose later" approach, not jumping into suggestions without context. Reconfirmed when delegating M16 with an explicit "Read @docs/ARCHITECTURE.md @docs/ACCEPTANCE_CRITERIA.md dan segala informasi lain yang bisa jadi kamu butuhkan di dalam @docs" instruction before implementation. Confidence: 0.75

- When asked to adjust/deploy to an external platform (e.g., "adjust source code backend agar bisa deploy di vercel"), explicitly instructs the agent to consult the platform's LATEST official documentation first ("coba baca dokumentasi vercel terbaru untuk deploy backend kita") and expects the resulting changes/decisions to be grounded in those current docs (framework detection, plan limits, serverless specifics) rather than in stale assumptions or prior setup — the research step is part of the deliverable, and its findings get cited in the summary. Confidence: 0.8
- Prioritizes shipping to end users (Play Store, web) before internal tooling (testing suite, CI/CD) — prefers a deploy-first sequencing where store/web deployment is ordered ahead of the traditional tests→CI→deploy pipeline; the app reaching users takes precedence over perfecting test suites and CI infrastructure. Confidence: 0.85

- Prefers continuous forward momentum — proceeding to the next milestone/task even when the current milestone hasn't been fully tested or verified (e.g., EAS build still pending), rather than waiting for full verification before moving on. Confidence: 0.8
- Uses EAS development builds for mobile testing rather than Expo Go — agent should assume standalone dev builds when diagnosing platform-specific issues (e.g., OAuth, deep linking), not Expo Go web-proxy behavior. Confidence: 0.85
- When debugging configuration issues, compares against known-working existing projects (e.g., referencing another Expo RN app's OAuth config) to determine what's actually required — uses "this works without X on my other project" as evidence to push back on unnecessary additions. Confidence: 0.85
- When a feature keeps failing across multiple attempts — mobile (Google sign-in, splash rendering: "ikutin .../uangku-app/apps/mobile saja sebagai referensi...") or backend deployment ("masih issue, samain aja deh kaya .../uangku-app/apps/server") — the user points to one of their own working projects as the authoritative reference to follow end-to-end; the expected response is to port the reference's proven implementation (library choice, config, architecture) wholesale rather than continue patching the failing approach. Reconfirmed when the user complained "ngeyel banget si kenapa ga ikutin persis uangku kaya appnya dari app module contohnya dan lain-lainnya" after the agent deviated from the reference — full parity with the reference, including its import structure, is expected, not a reasoned variant. Confidence: 0.95

- When the user pre-edits deployment/config files themselves to mirror a reference project exactly ("aku coba samain persis seperti uangku-app") and then delegates with "kalo masih ada yang perlu diadjust tolong adjust ya, atau yang ada perlu aku ketahui beritahu" (adjust what needs adjusting, or tell me anything I need to know), they are authorizing project-specific deviations from the literal reference — the agent should (1) inspect the actual working-tree state of the user's hand-edited files first (they may differ from HEAD or from the agent's own last version), (2) reconcile to the correct form for THIS project where a literal copy would break the deploy (e.g., uangku's `buildCommand: pnpm run build` → atur's `build:vercel`; inline AppModule → keep `createApp()` for the `/v1` prefix), and (3) explicitly report every deviation and anything the user needs to know back in the closing summary — why each deviation exists and what non-code steps remain (env vars, root directory). A literal copy that silently drops project-required config is a hidden trap the agent must catch and disclose, not apply blindly. Confidence: 0.75

- Treats any error/warning lines in the platform's deployment build log (e.g., Vercel logging `api/index.ts(31,24): error TS2339` type errors that the bundler ignores and the build still completes) as failures to fix — the user pastes the raw log with timestamps as evidence and expects a completely type-clean log after the fix. Also expects the agent to distinguish an expected 404 at the API root (`Cannot GET /` when all routes live under `/v1/*`) from a real runtime crash, and to verify the actual route (e.g., `/v1/health`) rather than treating the root 404 as a deploy failure. Confidence: 0.6
- Zero tolerance for TypeScript errors even when the project's own `tsc --noEmit` passes clean — the user's IDE runs stricter settings than the repo tsconfig (e.g., `useUnknownInCatchVariables`, `strictPropertyInitialization`) and surfaces errors the project build doesn't catch (e.g., `'error' is of type 'unknown'` in catch blocks, `Property 'token' has no initializer and is not definitely assigned in the constructor` on class-validator DTO fields). The expected response is to apply strict-mode-safe patterns anyway — `error instanceof Error ? error.message : String(error)` for catch variables and `!` definite assignment on decorator-validated DTO properties (kept consistent with existing DTOs in the project) — and to sweep the whole codebase (grep for `catch (error)`/`error.message`) to fix every instance of the same pattern, including sibling files not explicitly reported, then re-verify with tsc + build + eslint. Confidence: 0.7

- When the user pastes IDE errors that do NOT reproduce in the compiler (e.g., `Cannot find module '@nestjs/common'`/`'express'` in `backend/api/index.ts` while `tsc -p tsconfig.json` and `nest build` are clean), expects the agent to classify them as environment/LSP issues before touching code — verify with the actual build commands and confirm the deps/@types exist in `node_modules` (in a pnpm workspace, deps hoist into `backend/node_modules`, which the IDE's language server only resolves when the TS project is opened from the backend dir; opening the monorepo from the root produces spurious "cannot find module" errors). The fix is environment-level, not code: `pnpm install` at the workspace root, reload window / restart the TS server, or open the backend folder separately — the agent should state plainly that no code change is needed and hand over those steps. Contrast with strict-mode-only TS errors (above), which DO get defensive fixes. Confidence: 0.6

- When debugging cloud service integration (OAuth, APIs), cross-references local codebase configuration (.env, environment variables) against the cloud service's configured settings (e.g., Google Cloud Console authorized URIs, client IDs) and expects the agent to diagnose mismatches between the two rather than only debugging code logic. Confidence: 0.65
- When Google OAuth auth URLs are built, `login_hint` must be included with the user's email (fetched from DB) so Google pre-selects the user's account and does not prompt them to pick an account again — this is a standard Google OAuth best practice for apps that already have the user signed in. Reconfirmed when the user explicitly asked the calendar flow not to re-ask for Google account selection because the app already signs in with Google ("kan login ke atur perjalanan udah pake google sign in"). Confidence: 0.92

- For new Google integrations, prefers reusing the OAuth client credentials already present in `.env` rather than creating a separate OAuth client (user: "sama ko kaya yang sekarang di backend env" — same as the one already in backend env) — reuse the existing client and verify the console redirect URI accepts the new callback instead of requesting new credentials. Confidence: 0.6
- When Google OAuth returns 403 `access_denied` with a message like "belum menyelesaikan proses verifikasi Google", the root cause is typically the OAuth client being in **Testing** mode in Google Cloud Console and the user's email not being added as a **Test user** — this is a Google Cloud Console configuration issue, not a code bug. The agent should diagnose this and give the user exact steps to add their email in the OAuth consent screen's Test users section. Confidence: 0.85
- For mobile Google Sign-In (native Android/iOS builds), the redirect URI must be Google's installed-app format `com.googleusercontent.apps.<client-id-suffix>:/oauthredirect` — derived from the OAuth client ID — NOT a custom app scheme; the custom scheme (e.g., `aturperjalanan://`) is not registered in Google Cloud Console and triggers the "Akses diblokir: Error Otorisasi / redirect_uri_mismatch" policy block. The native scheme (and any custom scheme) must also be registered in `app.json` `scheme` so the intent filter can deep-link back, and the app's signing SHA-1 fingerprint must match what's registered on the Android OAuth client (EAS uses its own project keystore, not the local debug keystore). Confidence: 0.8

- Even with the correct `com.googleusercontent.apps.<client-id-suffix>:/oauthredirect` redirect in code, native Android Google Sign-In fails (`Error 400: invalid_request`, "Akses diblokir: Permintaan ... tidak valid", raw response "Custom URI scheme is not enabled for your Android client") when the Android OAuth client has NOT enabled the "Custom URI scheme" option in Google Cloud Console → Advanced settings. This is a manual console toggle the user must flip — no code workaround bypasses it: the web client ID rejects `com.googleusercontent.apps...` scheme URIs ("scheme URIs not allowed for WEB client type") and `https://auth.expo.io/...` only matches the web client (android client + auth.expo.io → redirect_uri_mismatch), so the answer is a console action with propagation wait, not a code change. Diagnose by curl-testing client_id + redirect_uri combinations directly against `https://accounts.google.com/o/oauth2/v2/auth` (HTTP status/redirect vs the returned error string) to prove the blocker is console config before touching code — the exact error text tells which side is misconfigured. Reconfirmed when the user re-reported the identical error after the library migration: the console toggle is required regardless of which OAuth library is used (expo-auth-session OR @react-native-google-signin), since both rely on the `com.googleusercontent.apps...:/oauthredirect` redirect — the agent re-verified via curl and handed exact console steps plus a rebuild note rather than touching code. Confidence: 0.85
- Prefers minimal, lean configuration — pushes back on adding unnecessary scopes, APIs, or settings ("scope belum perlu kali", "People API kenya gaperlu deh") and only adds what is strictly required. Confidence: 0.8

- Expects consistent naming conventions across test files — inconsistent test file naming (e.g., mixed `*.spec.ts`, `*.e2e-spec.ts`, `*.test.ts` patterns) is a bug that should be proactively audited and fixed. Confidence: 0.85

- When a fix changes data validity constraints in the backend, update test mock data to use realistic values that comply with production constraints (e.g., valid UUIDs where the DB column expects UUIDs) rather than keeping arbitrary mock identifiers — test data should reflect real data shapes. Confidence: 0.7

- When a fix changes expected runtime behavior (e.g., a resource state that was previously rejected is now permitted), the corresponding unit test assertion must be updated to match the new behavior — replace "rejects" assertions with "allows" assertions, not just leave stale expectations. Confidence: 0.8

- Expects the agent to proactively cross-reference milestone requirements against ALL project documentation (PRD, WORKFLOW, FIGMA, ARCHITECTURE, actual Figma screens) to find gaps — not just implement the milestone checklist verbatim. The user expects the agent to think critically about what's missing beyond what's explicitly written. Confidence: 0.8

- When the user reports a feature as "belum fungsional" (not functional) or asks to re-audit a flow, expects the agent to audit the whole chain end-to-end — UI trigger, confirmation path, hook, API route, backend service guards, query invalidation — and verify that the backend route is correct (e.g., creator-only guard) and that removing/updating data invalidates the right queries so the UI reflects it, then fix every broken link in the chain rather than only the visible symptom. Confirmed again when the user reported "votting tanggal masih belum bisa dibuat di button Buat Vottingnya" and the agent traced the root cause through the whole chain (sheet date-candidate seeding race → createPoll → listPolls → auto-created poll in createTrip) before fixing. Confirmed again when the user reported wishlist tags lingering as filters after a wishlist was endorsed to a trip or deleted ("tagsnya masih muncul sebagai filter di menu wishlist, seharusnya terhapus juga tagsnya") — the agent traced the full chain (backend tag derivation from active wishlists only + its spec test, then frontend React Query hook caching/invalidation) and fixed all four mutation hooks. Confidence: 0.85
- When the user reports a cross-cutting defect with systemic scope signals ("mode gelapnya belum terapply ke semua screen dan ada juga yang sudah apply tapi belum optimal" — dark mode not applied to all screens, and some that are applied are not optimal), expects the agent to audit the WHOLE codebase for the systemic cause (e.g., count every file bypassing the theme system) and fix it at the shared infrastructure layer (e.g., a reactive `colors` facade + migrating shared components) rather than patching the one screen reported — and then report honestly what fraction is fully covered vs. what remains (e.g., module-scope styles still light), including offering to continue the remaining migration. Confidence: 0.8

- Prefers incremental, module-by-module work — both for fixes (aligning web with mobile: "dari module auth dulu" — auth module first) and for building new feature areas one tab/section at a time ("kita fokus untuk tab Itinerary terlebih dahulu" — focus on the Itinerary tab first before moving to other tabs of the same screen) — rather than attempting sweeping cross-cutting changes all at once. Confidence: 0.7

- Expects the agent to proactively manage backend infrastructure (starting dev servers, killing zombie processes on occupied ports, verifying service health) when connection errors arise — treats "backend not running" as an agent responsibility, not a user task. Confidence: 0.7

- Reports infrastructure issues (connection refused, port conflicts) the same way as code bugs — expects the agent to autonomously distinguish between actual code problems and missing/stale services, and fix accordingly. Confidence: 0.65

- Prefers a structured "audit-first, fix-later" workflow when addressing UI/design discrepancies — first comprehensively audit and list all differences between Figma design and implementation in a detailed plan, then fix them systematically one by one, rather than making ad-hoc fixes without a prior comparison. When the user references specific Figma screen source files (e.g., `@figma/src/app/components/screens/Screen105Wishlist.tsx`, or a whole flow range like Screen36–41 Undang Teman, Screen97–102 Anggota Perjalanan, Screen103TripEdit, or Screen76Chat.tsx through Screen29PickDateCandidate2.tsx), those files are the authoritative spec — the audit must trace into the shared part components they import (e.g., `WishlistParts.tsx`, `InviteParts.tsx`, `ChatParts.tsx`) and align ALL discrepancies found, not just the reported symptom ("tolong audit lagi pastiin UInya akurat ke ...", "tolong lebih teliti lagi buat designnya akurat sesuai figma"). Confirmed again when the user said "UI Media belum sama persis seperti design ... tolong buat akurat" referencing `Screen93TripDocuments.tsx` and `Screen94MediaFromChat.tsx`. Confidence: 0.96

- When a Figma design file is the spec, the user treats it as authoritative but NOT infallible — explicitly expects the agent to catch and correct minor design errors rather than blindly replicating them ("design yang ada di figma saat ini bisa saja ada kesalahan minor, jadi hati-hati dan tetap terapkan yang menurut mu benar saat designnya ada kesalahan"). The agent should note the correction (e.g., fields the design implies but the type/serializer never fills, business rules the design contradicts) and implement the sensible version. Confidence: 0.75

- Form validation errors must clear reactively when the user edits the corresponding field — error indicators should not persist after the user begins correcting the input, even if the form hasn't been re-submitted. "When edited, the error should disappear" is the expected behavior. Confidence: 0.85

- For runtime/API debugging, expects fast diagnosis and immediate fixes — gets frustrated when the agent spends excessive time (15-20+ minutes) exploring/reading files without producing code changes. Prefers the agent to quickly form a hypothesis, apply the fix, and verify, rather than exhaustive exploration. "Audit-first" applies to design tasks, not runtime bug fixing. Confidence: 0.85

- When implementing a new feature that is a variant of an existing one (e.g., "same API as trips but filtered by user"), expects the agent to immediately recognize the pattern similarity and reuse the existing implementation rather than reinventing or experimenting with alternative approaches (e.g., raw SQL workarounds). "kan simple tinggal apinya sama dengan yang trips hanya saja di filter by user" — the agent should diagnose which part (BE/FE) needs the change, then make a targeted, pattern-consistent fix. Confidence: 0.9

- When building a whole new feature that is analogous to one the user has previously built and maintained, the user expects it to mirror the existing feature's implementation as the reference pattern — same form field structure/order, detail sheet, link handling (Google Maps + ref_links), cover handling, hooks, and component patterns — and may explicitly hint at this ("tambahan hint, seharusnya aktivitas itu hampir sama ya dengan wishlist yang sebelumnya kamu maintain" — activities should be almost identical to the previously maintained wishlist). Don't design the new feature from scratch or diverge structurally; reuse and adapt the established wishlist patterns end-to-end (form, detail, cover, links). Confidence: 0.8

- When stuck in a debugging loop (repeated failed fixes on a live server), prefers stepping back to run existing e2e tests first to verify environment health and isolate the problem, rather than continuing to try ad-hoc fixes against the running server. "Pelan-pelan mungkin bisa coba mulai dari run e2e testnya" — slow down, use tests as the systematic starting point. Confidence: 0.8

- When verifying an SDK/library behavior change, writes a minimal standalone script that replicates the exact production client configuration (same constructor options) and asserts the expected difference (e.g., default SDK presigned URL carries `x-amz-checksum-crc32`; with `requestChecksumCalculation: 'WHEN_REQUIRED'` it does not), running it directly with node — preferring config-replication proof over reasoning about types or docs. Also checks the installed SDK version to ground the diagnosis. Confidence: 0.75

- Shares full curl commands (copied from browser DevTools / mobile webview network tab) to help the agent reproduce and debug API issues — expects the agent to run them directly rather than asking for simplified reproduction steps. Confirmed again when reporting a CORS failure on profile-photo upload to R2 with the complete browser-originated PUT request (Referer, User-Agent, sec-ch-ua headers). Reconfirmed when re-reporting the poll "Validation failed" against the Vercel backend with a full cURL (headers, payload, response) — the agent used the payload verbatim to verify the schema locally and pinpoint that the deployed service was stale. Confidence: 0.87

- On this Windows dev machine, `curl` in `cmd.exe` is unreliable for testing JSON API endpoints: `curl --data-binary @file` sends the body in a form Nest/Express body-parser doesn't parse (the raw body string becomes the parsed object's key), yielding a misleading `Validation failed` from the Zod pipe — a false negative that makes a healthy backend look broken. Use PowerShell `Invoke-WebRequest`/`Invoke-RestMethod` with `-ContentType 'application/json'` for reliable JSON endpoint testing, and read the backend's own logs to confirm the request reached the controller (the app's real fetch/axios requests are unaffected). Confidence: 0.72

- When repeatedly restarting backend processes, the agent must verify port is actually free (confirm no stale process owns it) before starting a new one — EADDRINUSE from zombie processes causes confusing false negatives where old code handles requests while new code appears to fail. Agent should use `lsof -i :PORT` after killing to confirm PID ownership. Confidence: 0.8

- Prefers full-stack feature ownership — when implementing a feature, the agent must fix ALL necessary layers (mobile UI, mobile hooks, backend services, shared packages, tests) proactively, not just the layer mentioned in the request. The user explicitly expects "perbaiki semua sisi yang diperlukan entah dari mobile ataupun be" (fix all necessary sides whether mobile or backend). Confirmed again when the user asked that a link input be both "bisa disimpan ke DB dan fungsional di Mobilenya" (stored in DB AND functional in mobile) — the agent must verify the whole chain end-to-end (DB persistence → serializer → API → mobile hook payload → UI display), not just one end. The agent should autonomously diagnose which layers need changes across the entire stack. Confidence: 0.9

- When a user refreshes the browser and presses back, the app should handle the empty navigation stack gracefully by redirecting to the last visited tab (session-persisted) rather than hanging — the user explicitly chose "back to last tab" over "always home". Confidence: 0.9

- When a new feature derives/populates data for existing records (e.g., resolving cover thumbnails from stored maps links), existing records must be backfilled automatically (e.g., background resolve triggered on list read) rather than requiring users to recreate or manually edit old items — the user asked "apakah wishlist existing juga bisa?" expecting existing data to benefit from new features without manual work. Confirmed again when legacy `pub-*.r2.dev` avatar URLs already stored in the DB were fixed at read time via serializer resolution (extract key → presign) with no migration required. Confidence: 0.75

- When asked to fix reported bugs, expects the agent to also audit and fix additional related bugs / ineffective UX discovered during the work (comprehensive scope), not just the explicitly listed items — e.g., when flagging one raw ISO time display, the user asked "cek lagi ada ya yang render timenya masih kaya gitu kalo ada perbaiki ya" (check for other places rendering times the same way and fix them). Confirmed again when reporting one missing button loader and asking to check all API integrations ("tolong cek kembali semua integrasi API yang belum ada loadernya"). Confidence: 0.87

- Focus is both web and mobile, but the user currently tests on web — fixes must work on both platforms and be verified primarily on web. Confidence: 0.7
- Android is now a first-class test target, not an afterthought — the user discovered platform-specific issues only on Android ("di android baru ketauan belum ada safeviewnya, selama ini di web ga ketauan") and later reported an Android build with multiple keyboard/focus bugs ("di android baru ketauan" context). This session confirmed the user currently tests on Android too, not just web — fixes must account for Android keyboard/focus behavior and the native build, not only web. Reconfirmed when the user reported an Android-only modal layout bug with a terse platform-prefixed symptom ("issue di android modal buat/edit aktivitas dan voting cuma muncul header dan footer") expecting the agent to diagnose the platform-specific layout root cause (Yoga flex collapse) and fix it autonomously. Confidence: 0.8

- Android keyboard/focus handling is a systemic project-level concern, not per-screen: when the user reports a cluster of focus/keyboard bugs (search bar cannot be focused, chat composer covered by keyboard, modal inputs covered), the agent should (1) check app-level Android config for `edgeToEdgeEnabled: true` (Expo SDK 53+, matches the working reference project's config) — without it Android uses legacy layout where the keyboard can cover inputs and focus misbehaves; (2) audit every `KeyboardAvoidingView` to use `behavior={Platform.OS === 'ios' ? 'padding' : 'height'}` instead of `undefined` on Android; and (3) convert overlay views that contain text inputs (e.g., an invite-friends bottom sheet) into proper `Modal` + `KeyboardAvoidingView` so the keyboard layout is handled correctly and results below the input become visible. Confidence: 0.8

- Prefers dev-stage solutions that require no paid resources the user doesn't have — when setting up external services (e.g., email/SMTP), wants a path that works without purchasing a domain (e.g., Resend's built-in `onboarding@resend.dev` sender), with a clear separation between what's needed now for development vs. only at production. Confidence: 0.7

- Cost-averse regarding paid cloud APIs — explicitly reluctant to enable Google Cloud billing even after enabling the APIs ("aku takut kena charge"); expects the agent to propose free/no-billing alternatives or graceful degradation — ideally a free fallback that still delivers a working result (e.g., Yandex Static Maps as a billing-free wishlist cover when Google Maps APIs return 403), not just disabling the feature or requiring billing to be turned on. Confidence: 0.75

- For wishlist covers, the user expects the actual place photo — the Google Maps place-photo thumbnail, i.e. exactly the image that appears when the Maps link is shared on WhatsApp/social (the Open Graph preview: "kenapa kalo kita share link tersebut ke whatsapp misalnya, itu muncul thumbnail image dari lokasi maps tersebut. nah aku mau pake thumbnailnya itu agar jadi cover di wishlist"). A generated map render (e.g., Yandex static map) is explicitly not acceptable. The agent should replicate the social preview rather than settle for a map image — scraping `og:image` with a social-media bot User-Agent (facebookexternalhit/WhatsApp/Twitterbot) returns the real `lh3.googleusercontent.com` place photo, free, no API key or billing. The user re-verifies visually via screenshot and pushes back ("masih kaya gitu") whenever a card still shows the map fallback, so any fix must actually reach the stored/displayed data, not just the code path. The same gmaps-thumbnail-as-cover mechanism is expected for activities too, not just wishlist — the user explicitly required activity covers to be fillable from the Google Maps thumbnail "seperti wishlist" (like wishlist). Confidence: 0.92

- Prefers to fill in secrets/credentials (API keys, SMTP passwords) directly into `.env` files themselves rather than pasting them into the chat for the agent to handle — when a service setup needs secrets, the agent should give the exact env keys to fill, then verify they are set (without displaying the values) and continue with the next step. Confirmed again when the user reported "sudah aku isi backend envnya" after being told which key to fill. The self-fill preference extends to deployment-platform secrets: when the user asks "gimana cara aku set CRON_SECRET?" (how do I set this secret?), they expect exact per-platform steps (Vercel dashboard → Project → Settings → Environment Variables → Production; GitHub → repo → Settings → Secrets and variables → Actions), a command to generate the value (`openssl rand -hex 32`), and the explicit requirement that a secret shared between the runtime and the workflow must be identical in both places (mismatch → 401 on cron) — not the value pasted into chat for the agent to set. Confidence: 0.75

- The user starts/runs the backend server themselves (from `dist`), separate from agent-managed processes — when the agent leaves a background backend instance running, a subsequent user-initiated start fails with EADDRINUSE. The agent should check who owns the port (`lsof -i :PORT`) and coordinate with the user's own instance rather than assuming its background task is the only server or that a busy port is always a stale zombie. Confirmed again when the agent reminded the user to restart the backend after editing `r2.service.ts` because the user runs from `dist` (source edits don't take effect until the user rebuilds/restarts). Confidence: 0.6

- When a bug's fix has two parts — a code change and a one-time manual cloud-dashboard configuration (e.g., R2 bucket CORS rule) — the agent fixes the code part, verifies the fix in isolation (e.g., prove presigned URLs no longer carry the offending header), then hands the user an exact manual step: the precise JSON config and where to paste it (Cloudflare Dashboard → R2 → bucket → Settings → CORS), explicitly labeling what cannot be done from code. The manual step should also be documented in `.env.example` so it is not forgotten ("agar tidak terlupa"). When the user then reports the same error persisting ("masih kena error cors") with a fresh raw reproduction, the agent must re-verify where the blocker now lives before re-instructing: confirm the code fix actually reached the running build (e.g., grep the `dist` output the user runs from), live-check whether the bucket config exists AND whether the API token even has permission to read/modify it (e.g., `GetBucketCors` → `AccessDenied` means the token lacks CORS permission), and simulate the browser's preflight exactly (`OPTIONS` with the real `Origin` + `Access-Control-Request-Method` → 403 proves no CORS rule is installed) — so the agent can state definitively that only the manual dashboard step remains, not more code work. The loop continues even after the user does the manual step: the user reports back the exact current dashboard state (pasted CORS JSON showing only GET/HEAD) rather than a status summary, and the agent diffs it against the intended config to identify what's still missing (PUT) and hands back the corrected JSON. Confidence: 0.78

- User runs concurrent development sessions (separate cmdc/agent sessions in other terminals) working on different tabs/features in parallel — type errors or unexpected file changes outside the agent's current scope may come from the user's other active session. The user explicitly confirms this setup ("aku memerintahkan cmdc sesi lain di terminal lain untuk mengerjakan tab itinerary") and again when explaining that voting errors came from a parallel session ("aku menjalankan sesi cmdc di terminal lain untuk develop tab votting"). The agent should attribute such errors to the parallel session, NOT touch or "fix" those files, and verify only its own scope — e.g., running typecheck filtered to exclude the other session's area (grep -vE "itinerary") to prove its own files are clean without stepping on the other session. Exception: when the user explicitly routes a task to THIS session for a file another session owns (e.g., "sorry aku salah sesi tadi harusnya minta fix tab itinerary di sini. tolong perbaiki empty state itinerary"), the agent may edit that file, but must make only the minimal targeted change, verify it didn't clobber the other session's concurrent edits (re-read the lines / git diff), and check typecheck for that file afterwards. When the user instead realizes they misrouted a task to this session and says to refocus ("aku tadi salah sesi minta kamu kerjakan itinerary, harusnya kamu tetap fokus mengerjakan votting"), the agent should acknowledge the correction and redirect primary effort back to its assigned area, treating any remaining work on the misrouted feature as secondary. Confidence: 0.9

- Prefers tackling the easier/simpler task first when given a batch of changes, then proceeding to the larger refactor ("mulai dari yang mudah dulu"). Confidence: 0.55

- When the user reports "masih banyak bug dan ketidak sesuaian" (still many bugs and discrepancies) without enumerating them and asks "tolong teliti" (please be meticulous), expects the agent to start with a systematic automated audit of the recently changed code — full typecheck, typecheck filtered to the changed files, then ESLint — to discover and fix all issues proactively, rather than asking for a bug list or fixing only the visible symptoms. Confidence: 0.6

- When the root cause of a feature gap is invalid stored data (e.g., dead `maps.app.goo.gl` short links), prefers to fix the data directly (re-copying valid links via Google Maps Share) over having the agent add defensive handling code (e.g., dead-link detection UI) — given the choice, the user picks the no-code data-fix path, so the agent should present it first with exact step-by-step instructions and confirm the existing mechanism self-heals afterward. Confidence: 0.55
- Prefers atomic, end-to-end conversions between entities (e.g., wishlist → trip via a dedicated convert-to-trip endpoint that creates the parent, seeds the first child item with the source's full data — time, notes, location, maps/ref links, thumbnail — and soft-deletes the source), rather than a plain create that drops the source data. The user's mental model is that the source entity is conceptually also that child ("wishlist harusnya itu aktivitas juga kan"), so the converted wishlist must appear as the first activity in the itinerary with matching data. Confirmed again when the user asked for the time to also be prefilled in the conversion flow ("sebaiknya prefill juga jika bisa") and reported the carried-over activity time rendered incorrectly. Confidence: 0.75

- Typecheck, build, and unit-test green are NOT sufficient verification — the user expects a feature to actually work end-to-end in the UI and will challenge the agent with "ko bisa lolos pengecekan kamu?" (how could this pass your check?) when a shipped feature turns out non-functional. Verification must exercise the real flow (open the sheet, submit, observe the result), not just compile/test. Confidence: 0.75

- When a user responds to an invitation (accept/decline), the backend must resolve/update the corresponding notification so the UI stops showing actionable buttons (Terima/Tolak) and displays the resolved state — do not rely on the frontend marking the notification as read before responding, because the notification query filters out already-read notifications and the resolve never happens. The backend must update the notification payload (e.g., `{ resolved: true, accepted }`) when the invitation is responded to. Confidence: 0.85

- When a Prisma schema change (new column, e.g., `day_number`) is paired with a new migration, the agent must actually deploy the migration to the database (`npx prisma migrate deploy` / verify with `migrate status`) — an ORM client with a column the DB doesn't have causes 500 INTERNAL_SERVER_ERROR on the affected endpoints. Running `migrate dev` earlier is not sufficient; the migration can still be pending in the target DB and must be applied and re-verified before declaring the schema work done. Confidence: 0.75

- When implementing features involving dates/times (e.g., Google Calendar integration), the user stresses semantic correctness — "tolong kerjakan yang benar yaitu tanggal dan jamnya benar, dan info lainnya benar" (make it correct, the date and time must be right) — expecting proper handling of all-day events (exclusive end date = end_date + 1 day per Google rules) and timed events built from stored date + HH:MM as local wall-clock without timezone shifting, with unit tests asserting the exact start/end payloads sent to the external API. Confidence: 0.65

- When delegating a milestone, the user states its key business rules up front (e.g., calendar add only allowed once the trip date is fixed — not while voting; events only added to the user's own calendar) and expects them enforced consistently in both the UI (menu item disabled with explanatory subtitle) and the API (backend rejects non-fixed trips with a clear error), not just noted in the plan. Confidence: 0.6

- When removing a UI element (e.g., a summary line), the agent must also remove its now-unused variables, styles, and re-verify no stale references remain (grep the component for the removed identifiers) — the user's accepted workflow for deletions is "hapus UI, lalu hapus variabel yang tidak terpakai dan style" (remove UI first, then unused variables and styles). When the style sheet edit accidentally duplicated a block, the agent cleaned up the duplicates before declaring done. Confidence: 0.7

- Unit tests for scheduled/time-window logic (e.g., cron voting reminders) must assert the actual query filters — the exact `gte`/`lt` window per horizon — and mocks should mirror DB semantics (e.g., a `trip.findMany` mock that returns a trip only when the supplied `where` window matches its deadline, plus fixtures carrying realistic shapes like both `participant.userId` and `participant.user.id`). The audit explicitly flagged that the old spec "tidak mendeteksi bug window" (didn't detect the window bug) because it mocked `findMany` without asserting the filter — tests that would have caught a production bug must be rebuilt to do so. Confidence: 0.65

- When enhancing an external integration (e.g., Google Calendar event creation), prefers the outbound payload to carry as much relevant app data as possible — "sebanyak yang possible" — e.g., tags in the event summary, a description with creator/participants/date range, location derived from the itinerary, attendee emails with guest permissions (guestsCanModify/guestsCanSeeGuests), and a per-day itinerary summary — rather than a minimal payload. The agent should read the data schema to find all related fields and enrich the external payload with them, then update unit tests to assert the enriched fields and verify against a real record end-to-end. Confidence: 0.65

- The enriched external payload must be free of duplicated content — the user inspects the actual generated text and flags redundancy tersely ("tagnya double" — the tag line `Tag: #Staycation #Hotel` appeared both in the description header and again after the per-day itinerary). When composing generated descriptions/summaries, each piece of info (tags, participants, dates) must appear exactly once; duplication in the composer is a bug to fix in the shipped output, not left as-is. Confidence: 0.6

- When requesting a new feature (e.g., a new notification type like a trip start-time reminder), expects the change to land in BOTH the code and the project documentation — the user explicitly asks "tolong tambahin di dokumentasi dan di codenya ya" (add it to the docs and the code), so documentation (PRD/MILESTONES) is part of the deliverable, not an afterthought. Also expects the agent to first verify whether the feature already exists (checking existing infra like push notification/cron systems) before implementing, to avoid duplicating what's already built. Confidence: 0.7

- When implementing a new feature that extends an existing subsystem (e.g., adding a new notification/reminder type to an existing push-notification + cron system), the agent should reuse the established infrastructure end-to-end (same cron scheduling pattern, same notification-type enum, same in-app + push channel, same documentation pattern) rather than introducing a parallel mechanism — the new feature is treated as a variant of the existing one, and consistency with the current implementation is the default design. Confidence: 0.65
- Adapts milestone planning to practical constraints — when an already-paid resource (e.g., Play Console developer account) removes a cost barrier, the user merges the dependent milestone (e.g., app store release) into an earlier one (e.g., deployment) rather than maintaining the original roadmap structure; demonstrates preference for pragmatic milestone consolidation and leveraging sunk-cost infrastructure over rigid adherence to the initial plan. Confidence: 0.9

- When a milestone/task mixes implementable code/config work with procedural steps the agent cannot perform from code (cloud-dashboard setup, account actions), prefers a do-then-document sequencing: the agent executes the implementable code/config work first, then writes up the remaining manual/procedural steps as documentation afterward. Confirmed when the user said "kerjakan yang masih perlu dikerjakan di m18, dan jika sudah beritahu aku tata cara deployment..." (do the remaining work, then tell me the deployment process). Confidence: 0.85

- When a deploy command/script is renamed or shortened (e.g., the Vercel build chain moved into a `build:vercel` package.json script), expects every doc that quoted the old inline command to be updated to reference the new script — the agent proactively fixed DEPLOYMENT.md and README.md to say `pnpm run build:vercel` and noticed/fixed a numbering glitch it had introduced. Docs must always describe the current, working command. Confidence: 0.7

- Prefers complex multi-step operational procedures (deployment, cloud-dashboard config, store submission) to be consolidated into a dedicated markdown file under `docs/` (e.g., `docs/DEPLOYMENT.md`) rather than relayed purely in chat — explicitly requested via "taruh di sugam md" (put it in a md), covering architecture, env vars, per-service steps, verification, checklist, and troubleshooting in one reusable reference. Reconfirmed when the user asked for the Vercel deploy to be documented "di @docs/DEPLOYMENT.md secara detail step by step" and the agent rewrote §1.1–1.6 (prerequisites, dashboard/CLI steps, env table, build chain, manual migration ordering, cron, troubleshooting). Confidence: 0.92

- For local Android development builds, prefers `eas build --local` (EAS CLI local build — prebuild + Gradle on the machine — producing an installable expo-dev-client APK) over `expo run:android`; the user explicitly chose eas build --local when offered both methods. The build should be delivered as a reusable, documented shell script in `mobile/scripts/` registered as a pnpm convenience script (e.g., `build:android:dev:local`), with selectable env files (.env vs .env.development) so a dev APK installed on a physical device can point the API URL to the machine's LAN IP (localhost won't work on a device) — and build output goes to a gitignored folder (e.g., `dist/android-dev/`) separate from web export output. Confidence: 0.7

- When an EAS local build fails with `Error: Cannot overwrite directory '<dist>/android-dev' with non-directory '...app-debug.apk'`, the root cause is a stale artifact directory left in `mobile/dist/` from a previous (failed/interrupted) build — the EAS fs-extra copy refuses to overwrite an existing directory with a file. The fix is housekeeping (remove the stale `dist/android-dev/` directory and re-run `eas build --platform android --profile development --local`), not a code/config change — the agent should diagnose and clean it autonomously rather than treating it as a project misconfiguration. Confidence: 0.6

- When an EAS local build fails with `Error: Cannot overwrite directory '<dist>/android-dev' with non-directory '...app-debug.apk'`, the root cause is a stale artifact directory left in `mobile/dist/` from a previous (failed/interrupted) build — the EAS fs-extra copy refuses to overwrite an existing directory with a file. The fix is housekeeping (remove the stale `dist/android-dev/` directory and re-run `eas build --platform android --profile development --local`), not a code/config change — the agent should diagnose and clean it autonomously rather than treating it as a project misconfiguration. Confidence: 0.6

- For periodic background tasks on free/serverless hosting (uptime-pinging a free-tier service to prevent spin-down, hourly cron triggers to a serverless endpoint), prefers GitHub Actions scheduled workflows as the free in-repo mechanism — the agent sets them up in `.github/workflows/` rather than handing the user manual steps ("bisa kamu bantu atau harus aku manual?"), no paid uptime/cron service needed, with endpoint URL overridable via GitHub Variables and secrets via GitHub Secrets. Confidence: 0.7

- Before declaring a free-tier deployment strategy viable for a target user cap (e.g., 50 users), expects a per-layer quota analysis — hosting hours/RAM/cold start, DB size/MAU/Realtime connections, storage/egress, build limits — with a concrete verdict per layer plus explicit mitigations for free-tier quirks (spin-down & cold start, cron depending on service awake, blocked SMTP ports, project auto-pause after inactivity). Confidence: 0.6

- For EAS Builds, environment variables must come from EAS Environment Variables or committed env files — gitignored `.env` files do NOT reach the build (EAS archives the git repo), so a built APK can silently fall back to wrong/empty config (e.g., a hook falls back to the web OAuth client ID → Google blocks the native flow) while the same code works locally or on web. Each build profile in `eas.json` should declare `"environment"` (`development`/`preview`/`production`) so the build reads the matching EAS env set, and that env set must be created in EAS (`eas env:create <environment> --name ... --value ... --visibility plaintext|sensitive --non-interactive`). When a feature works on web but fails in a built APK, verify whether the env var was actually available at build time before assuming a code/config bug in the repo — a changed error message (e.g., "Error Otorisasi" → "Permintaan ... tidak valid") indicates the new code reached the build but an env mismatch remains. Confidence: 0.75

- Prefers build scripts to be fully self-contained one-command runners — every prerequisite step that must run before a build (regenerate brand assets, prebuild, env selection) is baked into the project's build scripts (`build:development:android` for EAS cloud, `build:android:dev:local` for local), so the user never runs separate manual steps. The user explicitly requested "kalo emang harus prebuild pokonya bikin semua yang harus dirun dulu dimasukkan ke script build development android tersebut. dan yang localnya" (if prebuild is needed, put every step that must run first into the build scripts — both cloud and local) after a cloud build produced an unchanged APK. Confidence: 0.8

- When an EAS cloud-built APK looks identical to a previous build, the root cause is usually that the changed assets/source are NOT committed — EAS cloud builds from git HEAD, not the working tree — not a missing prebuild (EAS performs prebuild automatically via CNG because the project has no committed `android/` folder). The fix is to commit the new assets first (the user agreed to "Commit asset dulu" when this was explained) or use `eas build --local`, which reads the working directory directly. Diagnose the commit state before assuming a prebuild/config problem. Confidence: 0.78

- Prefers generated brand assets to be reproducible from a committed script (SVG brand mark → PNG icon/adaptive-icon/favicon/splash via sharp) registered as a pnpm script (`generate:brand-assets`) and run automatically as the first step of the build scripts — assets always reflect the current brand source instead of stale hand-made placeholders, and the resulting PNGs are committed to the repo so cloud builds pick them up. Confidence: 0.65

- Prefers to keep `package.json` scripts lean and free of duplicates — when tidying scripts, remove redundant wrapper scripts and obsolete generators that produce the same result (e.g., dropped the `build:android:dev:local` shell wrapper in favor of a direct `eas build --local` script, and deleted `generate-placeholder-assets.cjs` superseded by `generate-brand-assets.cjs`). The user explicitly asked to "kurangi script yang resultnya sama di package.json" (reduce scripts that yield the same result). Confidence: 0.8

- Prefers scripts that read dependencies from `node_modules` to resolve pnpm-hoisted package paths robustly at runtime — scan `node_modules/.pnpm` for the package's directory prefix (e.g., `find(d => d.startsWith('@expo-google-fonts+plus-jakarta-sans@'))`) and throw a clear "run pnpm install" error when absent, rather than hardcoding a version-specific path like `.pnpm/@expo-google-fonts+plus-jakarta-sans@0.4.2` that breaks when the lockfile/version changes. Confidence: 0.8

- Before committing to a rendering technique in an asset generator (e.g., embedding a base64 `@font-face` font in SVG for sharp/libvips), verifies the library actually supports it with a minimal standalone node script that renders a tiny sample (e.g., sharp SVG → PNG) and asserts success — feasibility-probe first, then integrate, rather than assuming the technique works in the environment. Confidence: 0.7

- When restructuring a deployment or config, the user points to one of their own previously-deployed working projects as the authoritative pattern to mirror (e.g., "salah satu index.ts nest app yang udah aku deploy di vercel bisa liat di .../uangku-app/apps/server/api/index.ts") — the agent should copy that proven layout (api/ + vercel.json inside the app directory that is the Vercel Root Directory, entry importing from the app's own source/compiled output) rather than designing a novel structure. Confidence: 0.8

- When the user asks to confirm a build script will succeed ("pastikan bisa berhasil"), expects the agent to actually execute the full build end-to-end (even a ~17-minute local Gradle build) and verify the produced artifact (`ls -lh` + `file` on the APK), not just inspect package.json/eas.json/.env and reason that it should work. Confirmed when the user said "tolong cek sekali lagi, aku ingin run build:development:android:local pastikan bisa berhasil" and the agent ran the entire local EAS build until BUILD SUCCESSFUL and confirmed the APK. Confidence: 0.7

- When the user asks whether committing and pushing now will make a Vercel deploy succeed ("emangnya kalo aku commit push sekarang udah akan sukses vercel buildnya?"), expects a verified binary go/no-go answer — the agent must simulate the actual serverless runtime (e.g., load the compiled `dist` require graph under `node --no-experimental-strip-types` with `VERCEL=1` to prove cold-start load, after diffing the entry against the user's working reference project) rather than reasoning that the config looks right. The answer must clearly separate "build will succeed" from "function will run" and enumerate the remaining non-code prerequisites (set env vars, Root Directory = backend, Framework Preset = Other, commit only the backend files and not unrelated WIP) so the user knows exactly what still stands between push and success. Confidence: 0.6

- For local `eas build --local` pnpm scripts, pins an explicit `--output <file>` path (e.g., `dist/android-dev/app-debug.apk`) — without `--output`, EAS drops the APK in a temp build directory the user has to hunt for, and writing to a concrete file path (not a directory) sidesteps the stale-directory overwrite error seen when the output path was a folder. Confidence: 0.65

- When simulating Vercel's serverless bundling locally, uses esbuild (the actual builder behind Vercel's /api Node functions) rather than @vercel/ncc — ncc runs a full TypeScript program that type-checks every file in tsconfig (including unrelated `*.e2e-spec.ts` specs with their own typing errors), whereas esbuild transpiles without type-checking like the real deploy. Verify by bundling the api entry (`--bundle --platform=node --format=cjs --packages=external`), then loading it under the strict runtime that reproduces the crash (`node --no-experimental-strip-types`) with `VERCEL=1` set so the entry's `bootstrap()` guard is skipped and the require returns the handler instead of starting a listener. Confidence: 0.7

- When the user asks to remove a feature entirely ("remove dulu deh fitur dark mode dan semua source code terkait" — remove the dark mode feature and ALL its related source code), expects complete end-to-end removal of the feature's infrastructure, not just hiding the visible UI: the dark palette + reactive Proxy, the theme context/provider with its scheme/toggle/AsyncStorage persistence key, the settings toggle row + Switch, and the themed status bar logic — reverting `useTheme()` to a static light-only theme — then verifying via typecheck + grep that no dangling references remain (`colorsDark`, `themeDark`, `useThemeContext`, `toggleScheme`, `setActivePalette`, `colorsLight`, `Mode Gelap`, `ap_color_scheme`). The user prefers a clean single-theme app over keeping a half-supported feature, and expects the agent to plan and execute the full blast-radius removal autonomously, including checking which components still compile against the simplified theme API. Confidence: 0.85

- When the user pastes a build log containing pnpm workspace-configuration warnings (e.g., `pnpm.overrides` found in a sub-package's `package.json` — "This will not take effect. You should configure pnpm.overrides at the root of the workspace instead"), that is a request to eliminate the warnings, not ignore them as cosmetic. The expected fix is to relocate the workspace-level config to its correct home (root `package.json` or `pnpm-workspace.yaml`), remove it from the sub-package, validate the edited JSON files, and re-run the build to confirm it completes with zero warnings. Clean, warning-free build output is the bar. Confidence: 0.75

- For pnpm monorepo deploys, the lockfile must be kept in sync with root-level `pnpm.overrides` — when they diverge, `pnpm install --frozen-lockfile` (Vercel/CI default) aborts with `ERR_PNPM_LOCKFILE_CONFIG_MISMATCH` and blocks deploy. The fix is running `CI=true pnpm install --no-frozen-lockfile` at the workspace root (verify with `git diff --stat pnpm-lock.yaml` that the diff is minimal — just the overrides sync), committing the lockfile, and pushing. Confidence: 0.7

- When an install/build environment has `NODE_ENV=production` set, pnpm skips devDependencies (`devDependencies: skipped because NODE_ENV is set to production`) so the build fails with `tsc: command not found` (typescript/prisma are devDeps) — local installs/builds whose devDeps are required must run with `NODE_ENV=development pnpm install`; Vercel installs with a non-production NODE_ENV by default, so the trap is local-shell-specific but belongs in the deployment docs as a troubleshooting entry. Confidence: 0.7

- When delegating a batch of bug fixes from a markdown issue list (e.g., "LOOP: kerjakan perbaikan issue yang ada di @issues.md SATU per satu dan JANGAN BERHENTI hingga semua issue selesai dikerjakan, CHECKLIST issue yang telah selesai dikerjakan"), expects the agent to (1) process each issue one at a time in sequence, (2) NOT pause for confirmation between issues — work straight through until every issue in the file is done, and (3) maintain a visible checklist (todo list) that marks each issue completed as it finishes, then report the full completed checklist in the final summary. This batch-loop discipline applies to bug-fix batches, in contrast to large features which still go plan-then-confirm. Confidence: 0.9

- When consolidating Prisma migrations into a single init migration, prefers the reset-and-recreate approach over merging migration files — delete the `migrations` folder, run `prisma migrate reset --force --skip-seed` (drops all tables), then a fresh `prisma migrate dev --name init` — the user explicitly corrected the agent's merge plan with "bukan menggabungkan, tapi reset saja yang saat ini terus remove, terus tinggal migrate lagi kan?" (not merging, just reset and migrate again). Accepts the resulting data loss in development as the price of a clean, tidy single-migration history; the agent should still flag the destructive consequence of a shared DB (e.g., shared Supabase used by all envs) and get explicit confirmation before executing the reset. Confidence: 0.75

- When the user is actively testing locally ("aku masih tes local" — still testing locally, not against the deployed backend), the mobile app's `EXPO_PUBLIC_API_URL` must point at the local backend over the LAN (`http://<LAN-IP>:8080/v1`), not the deployed/production URL — the agent should fix `.env` to the local backend, keep `.env.example` in sync with a `<LAN-IP>` placeholder so the value stays portable across machines, and remind the user to restart Expo/Metro after env changes because `EXPO_PUBLIC_*` vars are inlined at bundle time. A malformed URL like `https://<private-IP>.vercel.app` (a Vercel subdomain carrying a private LAN IP) is an immediate red flag that breaks mobile requests with SSL handshake errors. Confidence: 0.65