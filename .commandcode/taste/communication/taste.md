- Delegates task prioritization to the agent — prefers open-ended prompts like "lanjutkan saja yang menurutmu paling tepat" (just continue with what you think is most appropriate) or simply "continue" to signal trust in the agent's judgment to autonomously decide the next priority, rather than micromanaging step-by-step direction. Confidence: 0.9

- For large features, accepts a structured two-phase approach: the agent first researches thoroughly (docs, Figma, code) and writes an implementation plan to a file (~/.commandcode/plans/), then pauses to confirm key architectural decisions via structured questions (e.g., OAuth-per-user vs service account, trigger point, validation behavior) before writing code — the user picks the recommended option ("keputusan sudah jelas: OAuth per-user, via menu ⋮, disabled saat voting") and the plan proceeds to execution. The plan itself is not a deliverable; the implemented milestone and its docs are. The same plan-then-confirm-then-execute flow also works for complex multi-layer bug fixes (avatar/photo propagation across the app), not just features — the user accepted a written plan plus an `ask_user_question` scope confirmation, then the implementation ran the full test/typecheck verification. Confidence: 0.7

- Reports bugs with systemic scope signals ("kemungkinan pada semua ts file" / "possibly in all ts files", "audit lagi segala hit api yang belum ada loadernya" — audit all API hits missing loaders) and expects the agent to autonomously diagnose and fix across the entire codebase, not just in isolated files — trusts the agent to determine the full blast radius without being guided file-by-file. Reconfirmed when the user listed four surfaces at once — avatar missing on the member page, trip cards, voting cards, and search results — expecting one fix to cover all of them. Confidence: 0.9

- Reports bugs with verbatim raw output from their tooling rather than paraphrasing — sometimes verbose (full error text with error codes like `Error 400: redirect_uri_mismatch` plus cloud service configuration details such as Google Cloud Console authorized URIs and client ID settings) and sometimes terse (a single DevTools console/network line such as `POST http://localhost:8080/v1/trips/{id}/polls 400 (Bad Request)` with no further explanation). In both forms the exact failing endpoint and status code are the diagnostic anchor, and the agent is expected to parse them and diagnose/fix autonomously without asking for clarification. Confirmed again when reporting "unggah foto profil kena cors" with only the raw curl, and again when pasting the R2 bucket's current CORS rule JSON with no explanation at all — the agent was expected to parse it and spot the missing `PUT` in `AllowedMethods` on its own. Confirmed again with "udah ga kena error cors, tapi urlnya gagal dirender jadi image" — a terse status-plus-symptom that expects the agent to diagnose the next link in the chain autonomously. Confirmed again when reporting a content bug in a Google Calendar event with a bare one-line summary ("tagnya double") followed by the verbatim generated description text ("berikut sample hasil detail yang dikirim menjadi event ke google calendar ... Tag: #Staycation #Hotel ... Tag: #Staycation #Hotel") — the raw sample is the evidence and the agent must spot the duplication in it. Confirmed again when reporting a blocked Google Sign-In on the mobile APK with the raw OAuth error text ("Akses diblokir: Error Otorisasi ... doesn't comply with Google OAuth 2.0 policy for keeping apps secure") plus the note that web already works. Confirmed again when pasting the Google Cloud Console OAuth client configuration verbatim (web client ID + authorized URIs, Android client ID + package name + SHA-1 fingerprint) as the authoritative reference for what is registered, expecting the agent to diff it against the code's client ID / redirect URI selection. Reconfirmed when re-reporting the still-blocked Google sign-in with the verbatim Google error-page text ("Akses diblokir: Permintaan Atur Perjalanan tidak valid", "Error 400: invalid_request itu udah route google gitu") with no explanation — expecting the agent to re-diagnose the console config autonomously. Confidence: 0.95

- Expects honest status updates when the agent is stuck — will directly ask "ada kendala apa?" (what's the obstacle?) if progress stalls. Prefers the agent to transparently report blockers and pivot strategies quickly rather than continuing to debug the same approach unproductively. Confidence: 0.8

- When reporting API/backend integration issues, provides the exact `curl` command with full HTTP headers (auth token, content-type, origin, user-agent) and payload as reproduction evidence — expects the agent to treat this as the definitive reference for the request format and cross-reference it against the backend schema/validation expectations (e.g., Zod schemas) rather than guessing the API contract or asking for more details. Confidence: 0.7
- When the user asks whether a feature is possible ("apakah itu tidak possible?"), expects the agent to verify external configuration (API keys, enabled cloud services) before answering — clearly distinguishing a missing-config blocker from an implementation gap and stating exactly what the user must do (e.g., fill `GOOGLE_MAPS_API_KEY`) to enable it. The verification must go beyond checking that a key is present: live-test the API to confirm it actually responds (e.g., billing enabled) — when the user reports the feature still doesn't work after filling the key, the agent should test the real endpoint and diagnose account-level blockers like Google Cloud billing not enabled, then explain these as non-code issues. Reconfirmed when calendar event creation kept failing after a successful OAuth consent: reproducing the exact Google Calendar API call with the stored token surfaced `403 accessNotConfigured / SERVICE_DISABLED` — the Calendar API was simply never enabled in the console — a one-click user action (Enable), not a code bug. Confidence: 0.85
- Reports UI bugs by attaching screenshots and expects the agent to visually identify the exact offending element from the image — "sadar ga itu height tagsnya masih tinggi banget" signals frustration when a previous fix targeted the wrong element instead of what the screenshot clearly showed (the tag filter chip, not the cards). Recurring follow-ups like "masih issue, lihat screenshot tersebut" / "masih kaya gitu" (still like that) with a fresh screenshot mean a previous fix was incomplete and the agent must re-examine the screenshot — and trace whether the running backend actually picked up the new code and re-populated stored data (not just the source) — to pinpoint what's still wrong. The agent should verify which element is actually wrong from the screenshot before fixing. Terse re-reports without a fresh screenshot ("UI unggah file media masih belum kamu kecilin", "media viewer masih issue selalu media pertama yang terbuka") equally signal that the previous fix didn't land — the agent must re-diagnose the real root cause (e.g., layout placement rather than the visible size) instead of re-applying the same fix. Confidence: 0.85


- When a feature isn't working (e.g., maps link → cover image), provides a concrete sample input (a real `maps.app.goo.gl` short link) plus the expected output (screenshot / target image URL) as acceptance criteria — expects the agent to support URL variants the current code can't parse (e.g., follow redirects on short links before extracting place_id/coords) and make that exact sample work end-to-end. When the same feature still fails after configuration, follows up with a brief status ("sudah aku isi backend envnya, tapi belum keisi cover") and expects the agent to debug directly against the live system rather than re-explain the setup. Confirmed again when the user paired a sample short link with the expected Google Maps thumbnail URL ("sample link maps volume hotel ..., link thumbnailnya ...") so the agent can reverse-engineer the target URL format. Confidence: 0.75

- When behavior differs across seemingly similar records ("kenapa cuma wishlist volume hotel? sedangkan yang lain yang punya gmaps juga tidak?"), expects the agent to verify each record's actual source data (live-test each stored link's og:image) and attribute the divergence to data characteristics rather than treating it as a code bug or re-checking the code path. The explanation should be data-driven and per-record — and must also verify whether each stored short link is still alive (404 vs 200), since a dead `maps.app.goo.gl` link is a distinct root cause from "the place has no photo". When the user disputes a diagnosis with concrete counter-evidence (a screenshot proving the place DOES have a photo, plus the actual `lh3.googleusercontent.com` thumbnail URL), that evidence is authoritative — the agent should dig deeper for the true cause (e.g., dead stored link) and revise the explanation instead of re-asserting the earlier conclusion. Confidence: 0.75
- When the user asks "menurutmu sebaiknya X atau tidak?" (should we keep/remove X?), they expect a considered recommendation with the tradeoff explained before any change is made ("tolong dipertimbangkan dulu") — present options with a clear recommendation and let the user decide, rather than executing unilaterally. Confidence: 0.7

- User-facing help/FAQ content must describe only what the app actually does — when asked to expand the FAQ and "pastikan seusai" (make sure it's accurate), claims about features not yet built (e.g., "sinkron ke kalender"/calendar sync) must be removed and each answer grounded in the real implemented behavior; contact links in such content must be tappable (`mailto:`). Confidence: 0.6

- When blocked by an external-service configuration (e.g., Google OAuth), the user shares screenshots of the cloud dashboard (e.g., Google Cloud Console OAuth consent screen showing the "Testing" status and the test-users list) as authoritative evidence of the current config state — the agent should read the screenshot (vision) and use the displayed state to give exact dashboard steps, clearly distinguishing config blockers from code bugs. Reconfirmed when the user pasted the OAuth consent screen showing "Testing" status with zero test users after reporting the 403 `access_denied` flow — the screenshot is authoritative evidence of the current config state. Confidence: 0.75

- When the user pastes a third-party tool's suggestion (e.g., a Gemini answer recommending a quick solution like the Google Calendar URL scheme), they expect the agent to critically evaluate it against the user's ACTUAL stated requirements — no redirects to Google, in-app success feedback — and pick the best approach for the project ("tolong pertimbangkan cara terbaik buat kita"), rather than blindly adopting the suggestion. A canned/quick solution that fails the user's real acceptance criteria should be rejected with the reasoning explained, even when it comes from an authoritative-sounding source. Confidence: 0.7

- For technical behavior tied to a specific code/config location, prefers a detailed, code-grounded explanation of what it does, why it exists, how it behaves step by step, and the practical implications (rather than a brief definition). Confidence: 0.75
figuration (separate client IDs, redirect URIs, package name) against that platform's OAuth requirements rather than debugging the shared code path. Confidence: 0.55
