import { ChatThreadView, DEMO_CHAT_MESSAGES } from '../trip/ChatParts';

/** Tab Chat — pesan grup, pipeline selaras detail perjalanan */
export function Screen76Chat() {
  return <ChatThreadView messages={DEMO_CHAT_MESSAGES} />;
}
